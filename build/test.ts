import { Core, Pad } from './esm/index';

const userModel: Core.Model.User = new Core.Model.User();

const padCollection: Pad.Collection.Diagram = new Pad.Collection.Diagram();

console.log(padCollection);
